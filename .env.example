# ------------------------------------------------------------------
# 資料庫連線字串
# 格式: mysql+pymysql://<帳號>:<密碼>@<主機>:<port>/<資料庫名稱>
# ------------------------------------------------------------------
DATABASE_URL=mysql+pymysql://<db_user>:<db_password>@<db_host>:3306/<db_name>

# ------------------------------------------------------------------
# CORS 允許的來源網域，多個用逗號分隔
# 本機測試範例：http://localhost:8080
# ------------------------------------------------------------------
ALLOWED_ORIGINS=http://localhost:8080

# ------------------------------------------------------------------
# FastAPI 掛在反向代理下的路徑前綴。本機測試留空即可；
# 部署到真實網域、有 nginx 反代路徑前綴時，在這裡填上該前綴
# ------------------------------------------------------------------
ROOT_PATH=

# ------------------------------------------------------------------
# 執行環境：local / staging / production
# ------------------------------------------------------------------
ENVIRONMENT=local
